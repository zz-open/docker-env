###########################################################
###################### General Setup ######################
###########################################################

### Drivers ################################################

# All volumes driver
VOLUMES_DRIVER=local

# All Networks driver
NETWORKS_DRIVER=bridge

# TimeZone ################################################
TZ=Asia/Shanghai

# Point to the path of your applications code on your host
APP_CODE_PATH_HOST=~/var/www

# Point to where the `APP_CODE_PATH_HOST` should be in the container
APP_CODE_PATH_CONTAINER=/var/www

# You may add flags to the path `:cached`, `:delegated`. When using Docker Sync add `:nocopy`
APP_CODE_CONTAINER_FLAG=:cached

# Choose storage path on your machine. For all storage systems
DATA_PATH_HOST=~/.docker-env/devops

# log storage path on your machine.
LOG_PATH_HOST=../logs/devops

# docker.sock file storage path on your machine.
DOCKER_SOCK_PATH_HOST=/var/run/docker.sock

### Docker Host IP ########################################

# Enter your Docker Host IP (will be appended to /etc/hosts). Default is `10.0.75.1`
DOCKER_HOST_IP=172.19.200.1

### Docker compose files ##################################

# Select which docker-compose files to include. If using docker-sync append `:docker-compose.sync.yml` at the end
COMPOSE_FILE=docker-compose.yml

# Change the separator from : to ; on Windows
COMPOSE_PATH_SEPARATOR=:

# Define the prefix of container names. This is useful if you have multiple projects that use laradock to have separate containers per project.
COMPOSE_PROJECT_NAME=docker-env-devops

###########################################################
################ Containers Customization #################
###########################################################

### Gitlab ###############################################

GITLAB_CONTAINER_NAME=gitlab
GITLAB_HOSTNAME=zz.gitlab.com
GITLAB_HOST_PATH=./gitlab
GITLAB_VERSION=latest
GITLAB_DOMAIN_NAME=http://zz.gitlab.com:8000
GITLAB_ROOT_PASSWORD=RootPassword!
GITLAB_HOST_HTTP_PORT=8000
GITLAB_HOST_HTTPS_PORT=8443
GITLAB_HOST_SSH_PORT=2224
GITLAB_FRONTEND_IP=172.19.140.110
GITLAB_BACKEND_IP=172.19.150.110

### GITLAB-RUNNER ###############################################

GITLAB_RUNNER_CONTAINER_NAME=gitlab-runner
GITLAB_RUNNER_HOSTNAME=gitlab-runner
GITLAB_RUNNER_HOST_PATH=./gitlab-runner
GITLAB_CI_SERVER_URL=http://localhost:8989
GITLAB_RUNNER_REGISTRATION_TOKEN="1234567890"
GITLAB_REGISTER_NON_INTERACTIVE=true
GITLAB_RUNNER_FRONTEND_IP=172.19.140.111
GITLAB_RUNNER_BACKEND_IP=172.19.150.111

### Jenkins ###############################################

JENKINS_CONTAINER_NAME=jenkins
JENKINS_HOSTNAME=jenkins
JENKINS_HOST_PATH=./jenkins
JENKINS_VERSION=lts
JENKINS_HOST_HTTP_PORT=8090
JENKINS_HOST_SLAVE_AGENT_PORT=50000
JENKINS_FRONTEND_IP=172.19.140.112
JENKINS_BACKEND_IP=172.19.150.112

### Application ###############################################

APPLICATION_CONTAINER_NAME=hello
APPLICATION_HOSTNAME=hello
APPLICATION_GO_VERSION=1.21.4
APPLICATION_HOST_PATH=./application
APPLICATION_MAIN_NAME=main.go
# 如果没有特殊要求，此处的二进制文件名不建议修改
APPLICATION_BIN_NAME=main
APPLICATION_HOST_HTTP_PORT=12000
APPLICATION_FRONTEND_IP=172.19.140.115
APPLICATION_BACKEND_IP=172.19.150.115
# 以下参数控制不同版本 START ####
APPLICATION_CODE_PATH_HOST=./hello/
APPLICATION_VERSION=v1.0.0
# 以下参数控制不同版本 END ####